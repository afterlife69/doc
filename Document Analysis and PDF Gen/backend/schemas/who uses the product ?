who uses the product  ?

    -students
    -teachers
    -users who want their content summarized or need a pdf for future reference


user schema ->

    user_id
    username
    firstname
    lastname
    email
    password hash
    documents_generated -> ids of documents in the s3 bucket

do we need another schema ? ig no? we do.


    _id of user.
    username
    firstname
    lastname
    email
    password hash
    documents_generated -> type document:

                                _id = filename in s3.
                                filename
    
    all r stored in one s3


